import styled from "styled-components";

export const JobTitle = styled.input.attrs({
  type: "text"
})``;

export const Location = styled.select``;

export const Picture  = styled.input.attrs({
  type: 'file'
})``;
